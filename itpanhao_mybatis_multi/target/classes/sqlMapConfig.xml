<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
<!--    通过properties标签加载外部properties文件-->
    <properties resource="jdbc.properties"></properties>
    <!--    自定义别名-->
    <typeAliases>
        <typeAlias type="com.panhao.domain.User" alias="user"></typeAlias>
        <typeAlias type="com.panhao.domain.Order" alias="order"></typeAlias>
        <typeAlias type="com.panhao.domain.Role" alias="role"></typeAlias>
    </typeAliases>
    <!--    自定义类型处理器-->
    <typeHandlers>
        <typeHandler handler="com.panhao.handler.DateTypeHandler"></typeHandler>
    </typeHandlers>
<!--    配置数据源环境-->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"></transactionManager>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driver}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>
<!--    加载映射文件-->
    <mappers>
        <mapper resource="com/panhao/mapper/UserMapper.xml"></mapper>
        <mapper resource="com/panhao/mapper/OrderMapper.xml"></mapper>
    </mappers>

</configuration>